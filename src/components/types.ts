export type Item = {
    name: string,
    price: number,
    quantity: number,
};

export type Delivery = {
    name: string,
    price: number,
};

export type Event = {
    title: string,
    date: string,
    price: number,
};